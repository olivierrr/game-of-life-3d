var THREE = require('n3d-threejs')

window.particleSystem
window.maxParticleCount = 200
window.particles

PARTICLE = {}


var create = function(context) {

    // context.P = Object.create(PARTICLE)
    // context.P.reset.call(context)
}

module.exports.create = create